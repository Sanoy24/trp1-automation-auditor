# =============================================================================
# Automaton Auditor Swarm — Environment Variables
# Copy this file to .env and fill in your values.  NEVER commit .env.
# =============================================================================

# --- Ollama Local LLM -------------------------------------------------------
# Base URL of your running Ollama instance (default: local).
OLLAMA_BASE_URL=http://localhost:11434

# Ollama model tag to use for detective synthesis and judicial reasoning.
# Pull with:  ollama pull minimax-m2.5:cloud
OLLAMA_MODEL=minimax-m2.5:cloud

# --- GitHub Access (optional) -----------------------------------------------
# Personal access token for cloning private repositories.
# Requires at minimum: read:repo scope.
# Leave empty to audit only public repositories.
GITHUB_TOKEN=

# --- LangSmith Observability (Optional but Recommended) ---------------------
# Set to "true" to enable LangSmith trace collection.
LANGCHAIN_TRACING_V2=true

# Your LangSmith API key (https://smith.langchain.com/settings).
LANGCHAIN_API_KEY=

# LangSmith project name — traces will appear under this project.
LANGCHAIN_PROJECT=automaton-auditor-swarm

# LangSmith ingestion endpoint (do not change unless using a self-hosted instance).
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com